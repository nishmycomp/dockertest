<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Invoice {{#if isOverdue}}Reminder{{else}}Notification{{/if}}</title>
</head>
<body style="font-family: Arial, sans-serif; background-color: #f6f8fa; color: #333; margin: 0; padding: 0;">
  <div class="container" style="max-width: 600px; margin: 40px auto; background-color: #ffffff; border-radius: 8px; padding: 30px; box-shadow: 0 0 4px rgba(0,0,0,0.1);">
    <h1 style="font-size: 20px; margin-bottom: 10px; text-align: center;">{{companyName}}</h1>

    <div class="amount" style="font-size: 24px; font-weight: bold; color: #222; text-align: center;">${{totalAmount}} AUD</div>
    {{#if isOverdue}}
    <div class="overdue" style="color: #d93025; font-weight: 600; text-align: center; margin-bottom: 5px;">Overdue — {{dueDate}}</div>
    {{else}}
    <div class="due-date" style="color: #666; font-weight: 600; text-align: center; margin-bottom: 5px;">Due — {{dueDate}}</div>
    {{/if}}
    <div class="invoice-num" style="text-align: center; margin-bottom: 20px; font-weight: 500;">Invoice #{{invoiceNumber}}</div>

    {{#if invoiceUrl}}
    <a href="{{invoiceUrl}}" class="button" style="display: block; width: 100%; background-color: #000; text-decoration: none; padding: 10px 0; border-radius: 8px; font-weight: 600; text-align: center; margin-top: 15px; margin-bottom: 40px; color: #ffffff;">View Invoice</a>
    {{/if}}

    <p style="line-height: 2; margin: 10px 0px;">Hi{{#if clientName}} {{clientName}}{{/if}},</p>
    {{#if isOverdue}}
    <p style="line-height: 2; margin: 10px 0px;">Thanks for working with us. Your bill for <strong>${{totalAmount}}</strong> was due on {{dueDate}}.</p>
    <p style="line-height: 2; margin: 10px 0px;">If you've already paid it, please ignore this email. If not, please do so as soon as possible.</p>
    {{else}}
    <p style="line-height: 2; margin: 10px 0px;">Thanks for working with us. Please find attached your invoice for <strong>${{totalAmount}}</strong> due on {{dueDate}}.</p>
    {{/if}}
    {{#if invoiceUrl}}
    <p style="line-height: 2; margin: 10px 0px;">To view your bill visit the link below:</p>
    <p style="line-height: 2; margin: 10px 0px;"><a href="{{invoiceUrl}}" target="_blank">{{invoiceUrl}}</a></p>
    {{/if}}

    {{#if customMessage}}
    <p style="line-height: 2; margin: 10px 0px;">{{customMessage}}</p>
    {{else}}
    <p style="line-height: 2; margin: 10px 0px;">If you have questions or want to arrange an alternative payment, please get in touch.</p>
    {{/if}}
    <p style="line-height: 2; margin: 10px 0px;">Thanks,<br>{{companyName}}</p>

    {{#if lineItems}}
    <table style="width: 100%; border-collapse: collapse; margin-top: 40px;" width="100%">
      <tr>
        <th style="text-align: left; padding: 10px 0; font-size: 14px; border-bottom: 1px solid #ddd; font-weight: 600;" align="left">Description</th>
        <th style="text-align: left; padding: 10px 0; font-size: 14px; border-bottom: 1px solid #ddd; font-weight: 600;" align="left">Amount</th>
      </tr>
      {{#each lineItems}}
      {{#if @first}}
      {{#if ../showAllItems}}
      <tr>
        <td style="text-align: left; padding: 10px 0; font-size: 14px;" align="left">{{this.description}}</td>
        <td style="text-align: left; padding: 10px 0; font-size: 14px;" align="left">${{this.amount}}</td>
      </tr>
      {{else}}
      {{#if @index}}<3{{/if}}
      <tr>
        <td style="text-align: left; padding: 10px 0; font-size: 14px;" align="left">{{this.description}}</td>
        <td style="text-align: left; padding: 10px 0; font-size: 14px;" align="left">${{this.amount}}</td>
      </tr>
      {{/if}}
      {{else}}
      {{#if ../showAllItems}}
      <tr>
        <td style="text-align: left; padding: 10px 0; font-size: 14px;" align="left">{{this.description}}</td>
        <td style="text-align: left; padding: 10px 0; font-size: 14px;" align="left">${{this.amount}}</td>
      </tr>
      {{else}}
      {{#if @index}}<3{{/if}}
      <tr>
        <td style="text-align: left; padding: 10px 0; font-size: 14px;" align="left">{{this.description}}</td>
        <td style="text-align: left; padding: 10px 0; font-size: 14px;" align="left">${{this.amount}}</td>
      </tr>
      {{/if}}
      {{/if}}
      {{/each}}
      {{#if remainingItemsCount}}
      <tr>
        <td style="text-align: left; padding: 10px 0; font-size: 14px;" align="left">+ {{remainingItemsCount}} more line items</td>
        <td style="text-align: left; padding: 10px 0; font-size: 14px;" align="left"></td>
      </tr>
      {{/if}}
      <tr>
        <td class="total" style="text-align: left; padding: 10px 0; font-weight: 700; font-size: 16px; padding-top: 10px;" align="left">Amount Due</td>
        <td class="total" style="text-align: left; padding: 10px 0; font-weight: 700; font-size: 16px; padding-top: 10px;" align="left">AUD ${{totalAmount}}</td>
      </tr>
    </table>
    {{/if}}

    {{#if invoiceUrl}}
    <a href="{{invoiceUrl}}" class="button" style="display: block; width: 100%; background-color: #000; text-decoration: none; padding: 10px 0; border-radius: 8px; font-weight: 600; text-align: center; margin-top: 15px; margin-bottom: 15px; color: #ffffff;">View Invoice</a>
    {{/if}}
  </div>
</body>
</html>

